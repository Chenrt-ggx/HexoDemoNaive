{
  "name": "hexo-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "hexo generate --config config.yml",
    "server": "hexo server --config config.yml",
    "deploy": "hexo deploy",
    "clean": "hexo clean"
  },
  "hexo": {
    "version": "6.3.0"
  },
  "dependencies": {
    "hexo": "^6.3.0",
    "hexo-renderer-markdown-it-plus": "^1.0.6",
    "hexo-restful-api": "^0.1.8",
    "hexo-server": "^3.0.0"
  },
  "devDependencies": {
    "prettier": "^2.8.3"
  }
}
